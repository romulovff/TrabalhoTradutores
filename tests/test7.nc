float first_thing;

set add_float() {
    read();
    read();
    write(10);
}

float second_thing;

set add_set() {
    read();
    int another_thing;
    if(for(1+1));
    write(1/2+5.3-1/4*6.25);
    writeln(20+50);
    write(EMPTY);
    writeln(EMPTY);
    write("a string example");
    writeln("a string example");
    write('A');
    writeln('A');
}

set subsum(set s, int target, int cur_sum, set ans) {
    set zero; set one; set two; set three;
    zero = EMPTY;
    one = succ(zero);
    write(succ(zero, one));
    writeln(succ(zero, one));
}

set test_return() {
    return 1;
    return 1.3;
    return 2+5;
    return add(x in s);
    return ans;
    return EMPTY;
    return "string";
    return 'C';
}