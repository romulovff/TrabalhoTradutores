//Linguagem não reconhecida

void funcao(set s) {
  set t;

  if(s && t) {
    writeln("Algum teste errado");
  }

  writeln(read(1));

  l = 0

  if(s || t) {
    writeln("Algum outro teste errado");
  }
}

set s;

int main() {
  funcao(s);
  return 0;
}
