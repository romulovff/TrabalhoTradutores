// Linguagem não reconhecida

set add_set() {
  set s;

  read();
  int another_thing;
  if(for(1+1));
  write(1/2);
  writeln(20+50);
  write(EMPTY);
  writeln(EMPTY);
  write("a string example");
  writeln("a string example");
  write('A');
  writeln('A');

  return s;
}

void main() {
  add_set();
}
