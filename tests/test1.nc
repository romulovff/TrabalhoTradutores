int a;

int add_int(set s, set i, int x) {
    int l;
    x = x + 1;
    a = 1;
    s = EMPTY;
    return 1;
    (2 + -2) * 3;
}

int b;
int l;

void add_float(set s, set i) {
    l = 1;
    writeln('\n');
}

int c;

set add_set(set s) {
    if(1 < 2)
      if(1 > 2){
        add_int(a,c,l);
      } else{
        if(1 > 2)
          writeln('o');
      }
    b = 1;
    return add(1 in s);
}

int main() {
    set s;
    set t;
    set o;
}
