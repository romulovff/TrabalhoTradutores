int a;

float l;

set add_int(set s, set i, int x) {
    x = x + 1;
    a = 1;
    s = EMPTY;
    return 1;
    l = (2 + 1) * 3;
}

int b;

set add_float(set s, set i) {
    l = 1.1;
    writeln('\n');
    return add(-5.4 in add(1.5 in s));
}

int c;

set add_set(set s) {
    if(1 < 2)
      if(1 > 2){
        add_int(a,c,l);
      } else{
        if(1 > 2)
          writeln('o');
      }
    b = 1;
    return add(1 in s);
}

int main() {
    set s;
    set t;
    set o;
}
